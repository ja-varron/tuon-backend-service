[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"  # works on any setuptools version

[project]
name = "tuon"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
anyio_mode = "auto"